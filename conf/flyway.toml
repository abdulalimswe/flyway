[flyway]
locations = ["filesystem:migrations"]

[environments.default]
locations = ["filesystem:migrations"]
url = "jdbc:postgresql://localhost:5432/testflyway"
user = "postgres"
password = "mysecretpassword"
